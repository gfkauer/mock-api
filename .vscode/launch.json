{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Server",
            "program": "${workspaceFolder}/project/server.js",
            "cwd": "${workspaceFolder}/project",
            "preLaunchTask": "npm: install - project"
        }
    ]
}